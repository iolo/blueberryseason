doctype 5
- var posts = result
html
  head
    meta(charset="utf8")
    title BlueBerrySeason with Express
  body
    h1 BlueBerrySeason - Posts

    p #{posts.length} posts

    table(border="1")
      thead
        tr
          th Id
          th Title
          th Author
          th Modified
          th #Comments
      each post in posts
        tbody
          tr
            td
              a(href="show?postId=#{post.id}") #{post.id}
            td
              a(href="show?postId=#{post.id}") #{post.title}
            td #{post.author}
            td #{post.modified}
            td #{post.commentsCount}

    div
      a(href="new") New Post
